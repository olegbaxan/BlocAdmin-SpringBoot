package md.step.BlocAdmin.repository;

import md.step.BlocAdmin.model.Withdrawn;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WithdrawnRepository extends JpaRepository<Withdrawn, Integer> {
}
